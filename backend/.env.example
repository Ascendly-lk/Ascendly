# Supabase Configuration
# Get from: Supabase Dashboard → Settings → API
SUPABASE_URL=your-supabase-project-url
SUPABASE_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# Database URL (PostgreSQL)
# Get from: Supabase Dashboard → Settings → Database → Connection string
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# ── AI Provider ──────────────────────────────────────────────────────────────
# Set to "openai" or "azure" — only one provider is active at a time.
AI_PROVIDER=openai

# ── OpenAI (required when AI_PROVIDER=openai) ────────────────────────────────
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4o

# ── Azure OpenAI (required when AI_PROVIDER=azure) ──────────────────────────
# Get from: Azure Portal → Azure OpenAI → Keys and Endpoint
AZURE_API_KEY=your-azure-openai-api-key
AZURE_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_API_VERSION=2024-02-15-preview
AZURE_DEPLOYMENT_NAME=gpt-4o

# ── Optional: Google ADK / benchmark sub-agents ─────────────────────────────
GEMINI_API_KEY=your-gemini-api-key
GOOGLE_ADK_MODEL=gemini-2.0-flash
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-key
SERPAPI_API_KEY=your-serpapi-key

# ── Thesys C1 — interactive analysis UI ─────────────────────────────────────
# Get from: console.thesys.dev → Settings → API Keys
THESYS_API_KEY=your-thesys-api-key
THESYS_C1_MODEL=c1-exp/anthropic/claude-haiku-4.5
